import React from "react";
import { Card } from "@keel/design-system";

export function TimeTrackerPage() {
  return (
    <Card>
      <h1 className="text-3xl font-bold">Time Tracker</h1>
      <p className="text-muted-foreground">Placeholder: Time tracking interface (SUBMIT_TIMESHEET intent)</p>
    </Card>
  );
}
